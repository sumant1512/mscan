"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.migrateToCreators = migrateToCreators;
exports.default = default_1;
const ts = __importStar(require("typescript"));
const schematics_1 = require("@angular-devkit/schematics");
const schematics_core_1 = require("../../schematics-core");
function migrateToCreators() {
    return (tree) => {
        (0, schematics_core_1.visitTSSourceFiles)(tree, (sourceFile) => {
            const effectsPerClass = sourceFile.statements
                .filter(ts.isClassDeclaration)
                .map((clas) => clas.members.filter(ts.isPropertyDeclaration).filter((property) => {
                const decorators = ts.getDecorators(property);
                return decorators && decorators.some(isEffectDecorator);
            }));
            const effects = effectsPerClass.reduce((acc, effects) => acc.concat(effects), []);
            const createEffectsChanges = replaceEffectDecorators(tree, sourceFile, effects);
            const importChanges = (0, schematics_core_1.replaceImport)(sourceFile, sourceFile.fileName, '@ngrx/effects', 'Effect', 'createEffect');
            (0, schematics_core_1.commitChanges)(tree, sourceFile.fileName, [
                ...importChanges,
                ...createEffectsChanges,
            ]);
        });
    };
}
function replaceEffectDecorators(host, sourceFile, effects) {
    const inserts = effects
        .map((effect) => {
        if (!effect.initializer) {
            return [];
        }
        const decorator = (ts.getDecorators(effect) || []).find(isEffectDecorator);
        if (!decorator) {
            return [];
        }
        if (effect.initializer.getText().includes('createEffect')) {
            return [];
        }
        const effectArguments = getDispatchProperties(host, sourceFile.text, decorator);
        const end = effectArguments ? `, ${effectArguments})` : ')';
        return [
            new schematics_core_1.InsertChange(sourceFile.fileName, effect.initializer.pos, ' createEffect(() =>'),
            new schematics_core_1.InsertChange(sourceFile.fileName, effect.initializer.end, end),
        ];
    })
        .reduce((acc, inserts) => acc.concat(inserts), []);
    const removes = effects
        .map((effect) => ts.getDecorators(effect))
        .map((decorators) => {
        if (!decorators) {
            return [];
        }
        const effectDecorators = decorators.filter(isEffectDecorator);
        return effectDecorators.map((decorator) => {
            return new schematics_core_1.RemoveChange(sourceFile.fileName, decorator.expression.pos - 1, // also get the @ sign
            decorator.expression.end);
        });
    })
        .reduce((acc, removes) => acc.concat(removes), []);
    return [...inserts, ...removes];
}
function isEffectDecorator(decorator) {
    return (ts.isCallExpression(decorator.expression) &&
        ts.isIdentifier(decorator.expression.expression) &&
        decorator.expression.expression.text === 'Effect');
}
function getDispatchProperties(host, fileContent, decorator) {
    if (!decorator.expression || !ts.isCallExpression(decorator.expression)) {
        return '';
    }
    // just copy the effect properties
    const args = fileContent
        .substring(decorator.expression.arguments.pos, decorator.expression.arguments.end)
        .trim();
    return args;
}
function default_1() {
    return (0, schematics_1.chain)([migrateToCreators()]);
}
//# sourceMappingURL=index.js.map