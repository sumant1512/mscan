"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.migrateConcatLatestFromImport = migrateConcatLatestFromImport;
exports.default = default_1;
const ts = __importStar(require("typescript"));
const schematics_1 = require("@angular-devkit/schematics");
const schematics_core_1 = require("../../schematics-core");
const change_1 = require("../../schematics-core/utility/change");
function migrateConcatLatestFromImport() {
    return (tree, ctx) => {
        (0, schematics_core_1.addPackageToPackageJson)(tree, 'dependencies', '@ngrx/operators', '^18.0.0');
        (0, schematics_core_1.visitTSSourceFiles)(tree, (sourceFile) => {
            const importDeclarations = new Array();
            getImportDeclarations(sourceFile, importDeclarations);
            const effectsImportsAndDeclarations = importDeclarations
                .map((effectsImportDeclaration) => {
                const effectsImports = getEffectsNamedBinding(effectsImportDeclaration);
                if (effectsImports) {
                    if (effectsImports.elements.some((element) => element.name.getText() === 'concatLatestFrom')) {
                        return { effectsImports, effectsImportDeclaration };
                    }
                    return undefined;
                }
                else {
                    return undefined;
                }
            })
                .filter(Boolean);
            if (effectsImportsAndDeclarations.length === 0) {
                return;
            }
            else if (effectsImportsAndDeclarations.length > 1) {
                ctx.logger.info('[@ngrx/effects] Skipping because of multiple `concatLatestFrom` imports');
                return;
            }
            const [effectsImportsAndDeclaration] = effectsImportsAndDeclarations;
            if (!effectsImportsAndDeclaration) {
                return;
            }
            const { effectsImports, effectsImportDeclaration } = effectsImportsAndDeclaration;
            const operatorsImportDeclaration = importDeclarations.find((node) => node.moduleSpecifier.getText().includes('@ngrx/operators'));
            const otherEffectsImports = effectsImports.elements
                .filter((element) => element.name.getText() !== 'concatLatestFrom')
                .map((element) => element.name.getText())
                .join(', ');
            const changes = [];
            // Remove `concatLatestFrom` from @ngrx/effects and leave the other imports
            if (otherEffectsImports) {
                changes.push((0, schematics_core_1.createReplaceChange)(sourceFile, effectsImportDeclaration, effectsImportDeclaration.getText(), `import { ${otherEffectsImports} } from '@ngrx/effects';`));
            }
            // Remove complete @ngrx/effects import because it contains only `concatLatestFrom`
            else {
                changes.push((0, change_1.createRemoveChange)(sourceFile, effectsImportDeclaration, effectsImportDeclaration.getStart(), effectsImportDeclaration.getEnd() + 1));
            }
            let importAppendedInExistingDeclaration = false;
            if (operatorsImportDeclaration?.importClause?.namedBindings) {
                const bindings = operatorsImportDeclaration.importClause.namedBindings;
                if (ts.isNamedImports(bindings)) {
                    // Add import to existing @ngrx/operators
                    const updatedImports = [
                        ...bindings.elements.map((element) => element.name.getText()),
                        'concatLatestFrom',
                    ];
                    const newOperatorsImport = `import { ${updatedImports.join(', ')} } from '@ngrx/operators';`;
                    changes.push((0, schematics_core_1.createReplaceChange)(sourceFile, operatorsImportDeclaration, operatorsImportDeclaration.getText(), newOperatorsImport));
                    importAppendedInExistingDeclaration = true;
                }
            }
            if (!importAppendedInExistingDeclaration) {
                // Add new @ngrx/operators import line
                const newOperatorsImport = `import { concatLatestFrom } from '@ngrx/operators';`;
                changes.push(new schematics_core_1.InsertChange(sourceFile.fileName, effectsImportDeclaration.getEnd() + 1, `${newOperatorsImport}\n` // not os-independent for snapshot tests
                ));
            }
            (0, schematics_core_1.commitChanges)(tree, sourceFile.fileName, changes);
            if (changes.length) {
                ctx.logger.info(`[@ngrx/effects] Updated concatLatestFrom to import from '@ngrx/operators'`);
            }
        });
    };
}
function getImportDeclarations(node, imports) {
    if (ts.isImportDeclaration(node)) {
        imports.push(node);
    }
    ts.forEachChild(node, (childNode) => getImportDeclarations(childNode, imports));
}
function getEffectsNamedBinding(node) {
    const namedBindings = node?.importClause?.namedBindings;
    if (node.moduleSpecifier.getText().includes('@ngrx/effects') &&
        namedBindings &&
        ts.isNamedImports(namedBindings)) {
        return namedBindings;
    }
    return null;
}
function default_1() {
    return (0, schematics_1.chain)([migrateConcatLatestFromImport()]);
}
//# sourceMappingURL=index.js.map