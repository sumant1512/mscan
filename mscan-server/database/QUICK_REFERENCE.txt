╔════════════════════════════════════════════════════════════════════════╗
║                 MSCAN DATABASE MANAGEMENT - NPM SCRIPTS                ║
╚════════════════════════════════════════════════════════════════════════╝

📋 ALL COMMANDS:

  npm run db                  → Interactive menu (easiest!)
  npm run db:setup            → Setup fresh database
  npm run db:clean-tenant     → Clean data, keep tenants
  npm run db:clean-all        → Clean data, keep super admin
  npm run db:drop             → Drop entire database
  npm run db:reset            → Drop + Setup (fresh start)

─────────────────────────────────────────────────────────────────────────

🚀 QUICK START:

  First time?          → npm run db:setup
  Need fresh start?    → npm run db:reset
  Not sure?            → npm run db

─────────────────────────────────────────────────────────────────────────

📊 WHAT EACH COMMAND KEEPS/DELETES:

  Command              │ Database │ Tables │ Tenants │ Super Admin │ Data
  ─────────────────────┼──────────┼────────┼─────────┼─────────────┼──────
  db:setup             │ ✅ Create │ ✅ Create │    -    │  ✅ Create  │  -
  db:clean-tenant      │ ✅ Keep   │ ✅ Keep   │ ✅ Keep  │  ✅ Keep    │ ❌ Del
  db:clean-all         │ ✅ Keep   │ ✅ Keep   │ ❌ Delete│  ✅ Keep    │ ❌ Del
  db:drop              │ ❌ Drop   │ ❌ Drop   │ ❌ Delete│  ❌ Delete  │ ❌ Del
  db:reset             │ ♻️ Reset  │ ♻️ Reset │    -    │  ✅ Create  │  -

─────────────────────────────────────────────────────────────────────────

💡 COMMON WORKFLOWS:

  Daily fresh start:
    $ npm run db:reset && npm start

  Clean test data:
    $ npm run db:clean-all

  Reset but keep tenants:
    $ npm run db:clean-tenant

─────────────────────────────────────────────────────────────────────────
